# Crie uma função que permita contar o número de vezes que
# aparece uma letra em uma string.
import funcoesEx as fn


algo = input('Digite algo: ')
letra = input('qual a letra que quer contar: ')

print(fn.contador(algo, letra))
